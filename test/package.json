{
  "name": "myspot-rules-tests",
  "description": "Firestore security-rules unit tests (run against the emulator).",
  "private": true,
  "scripts": {
    "test": "mocha --timeout 15000 firestore.rules.test.js"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.1.0",
    "mocha": "^10.8.2"
  }
}
