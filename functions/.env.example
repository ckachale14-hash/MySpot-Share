# Non-secret config for Cloud Functions (committed as an example).
# Copy to functions/.env and adjust per environment. The real .env is gitignored.
#
# OpenAI model IDs by tier — keep IDs here (or in Remote Config), never hardcoded.
# Pick current OpenAI model IDs for each tier (a cheap "mini", a "standard",
# and a "flagship" for long-form). Review periodically as OpenAI ships newer models.
AI_MODEL_FAST=gpt-4o-mini
AI_MODEL_STANDARD=gpt-4o
AI_MODEL_PREMIUM=gpt-4o
AI_MODERATION_MODEL=omni-moderation-latest
AI_IMAGE_MODEL=gpt-image-1

# AI video provider (generic): submit endpoint + status endpoint + model id.
# Point these at OpenAI video / Runway / Pika / Veo. Leave blank to disable.
AI_VIDEO_API_URL=
AI_VIDEO_STATUS_URL=
AI_VIDEO_MODEL=

# Agora app ID is public (not a secret); the certificate is a secret (set via secrets:set).
AGORA_APP_ID=REPLACE_WITH_AGORA_APP_ID

# SECRETS DO NOT GO HERE. Set them via Secret Manager, e.g.:
#   firebase functions:secrets:set OPENAI_API_KEY
#   firebase functions:secrets:set PAYSTACK_SECRET            # Paystack secret key (init + webhook HMAC)
#   firebase functions:secrets:set FLUTTERWAVE_WEBHOOK_HASH   # Flutterwave verif-hash value
#   firebase functions:secrets:set STRIPE_SECRET
#   firebase functions:secrets:set STRIPE_WEBHOOK_SECRET
#   firebase functions:secrets:set AGORA_APP_CERTIFICATE   # Agora RTC token signing
#   firebase functions:secrets:set AI_VIDEO_API_KEY        # video provider key
#   firebase functions:secrets:set REVENUECAT_WEBHOOK_AUTH # RevenueCat webhook Authorization header
